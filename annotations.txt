// Projeto Slide do curso ORIGAMID ES6+ | Módulo 12 (Slide)

// Aula 1209 (Slide Pagination)

class SlideNav {
  // another methods...

  eventControl(item, index) {
    item.addEventListener('click', (event) => {
      event.preventDefault();
      this.changeSlide(index);
    });
  }

  addControl(customControl) {
    this.control = document.querySelector(customControl) || this.createControl();
    this.controlArray = [...this.control.children];
    this.controlArray.forEach((this.eventControl));
  }
}

// [row 18th]: 'this.controlArray.forEach((this.eventControl));'
// Como a assinatura do método 'eventControl(item, index){}' preconiza
// o comportamento do método 'forEach', basta passá-lo como arg0 desse
// método.
